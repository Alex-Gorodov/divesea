export enum AppRoute {
  Root = "/divesea",
  Discover = "/divesea/discover",
  Creators = "/divesea/creators",
  Sell = "/divesea/sell",
  Stats = "/divesea/stats",
}

export enum HeroItemSizes {
  Active = 392,
  Inactive = 318
}
